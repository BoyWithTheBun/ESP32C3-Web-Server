# ESP32C3-Web-Server
An algorithm that hosts a web server on an ESP32, and updates the webpage with a value that increases by one. Uses ESP32 webserver libraries to initialize a website on the local IP address, and by copying and pasting it into a web browser it will display the webpage.  
